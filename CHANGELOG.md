# 更新日志

本文件记录源码、工具链和使用方式的主要变化。所有版本目前均为预览版，不代表稳定版或收益保证。

## v0.5.0-preview.3 — 2026-09-23

- 防止东方财富停牌股成交额字段 `f6` 返回 `-`、`--` 或空值时中断板块统计；无效成交额不计入板块强势数量。
- 实时看板依据上证指数日 K 线识别非交易日并跳过自动筛选；盘前尚未确认时延后初筛，开盘后重新查询。
- 全量单元测试运行 132 项，131 项通过，1 项按环境跳过。

## v0.5.0-docker.2 — 2026-09-23

- 与 `v0.5.0-preview.3` 使用同一源码，提供 `linux/amd64` 与 `linux/arm64` 多架构 Docker 镜像。
- Docker 镜像由 GitHub Actions 构建并发布到 GitHub Container Registry。

## v0.5.0-preview.2 — 2026-09-22

- 防止东财资金流字段在开盘早期返回 `-`/`--` 时把字符串带入算术，避免筛选引擎 `TypeError`。
- 修复实时看板“低开洗盘”和“板块”标签映射，避免点击后清空高亮或进入未知标签页。
- 加强 Windows 工作台兼容性：控制台输出按 GBK/UTF-8 安全解码，启动器改为 ASCII + CRLF，并将 `tzdata` 纳入依赖；实时看板重启时不重复打开浏览器标签。

## v0.5.0-docker.1 — 2026-09-22

- 与 `v0.5.0-preview.2` 使用同一源码，提供 `linux/amd64` 与 `linux/arm64` 多架构 Docker 镜像。
- Docker 镜像由 GitHub Actions 构建并发布到 GitHub Container Registry。

## v0.5.0-preview.1 — 2026-09-21

- 新增 B/S Web 工作台 `daily-stock-analysis/scripts/web_workbench.py`：单端口 8765 同时提供一次性筛选工作台、报告库、工具箱（行情/基本面/报告扫描/持仓/T+1 验证/单股跟踪）与原版实时看板；跨平台，Windows 可用（需 `tzdata` 包）。
- 前端 `workbench_static/`（原生 HTML/JS/CSS，无外部依赖）；新增 Windows 启动器 `启动工作台.bat`；详见 `docs/web-workbench.md`。
- 手动筛选任务与看板自动刷新共用筛选锁；超时采用僵尸收割机制（引擎线程真正结束后才释放锁），避免数据竞争；报告读取和扫描限制在 `筛选结果/` 内防路径穿越；查询参数支持 UTF-8/GBK 双解码。
- 工作台默认仅监听本机、拒绝通配符 CORS，并避免在 API 响应中返回绝对路径；Docker 同时提供工作台和原版看板，宿主机端口默认绑定 `127.0.0.1`。
- 新增 Web 工作台安全回归测试；全套单元测试 116 项通过（1 项按环境跳过）。
- 已知边界：收盘后（15:00+）全模式筛选在 Windows 上可能超过 10 分钟（东财接口行为变化），建议收盘后仅跑 `strict` 单模式；盘中实测 strict 约 90 秒。

## v0.4.0-preview.2 — 2026-09-21

- 将严格池、趋势观察、低吸分类与评分、板块共振、资金排序和实时风控阈值统一收口到 `tools/rule_config.py`，保持原有现值与边界。
- CLI 与实时引擎共用低吸输出门槛、排序目标和尾盘时段，避免同一快照出现两套筛选口径。
- 登记“主力>15% + 超单>5000万 + 板块共振≥3”的回踩放宽实验，默认关闭且仅允许模拟仓；补充一致性校验与回归测试。
- 114 项单元测试通过；编译检查、一致性检查和 Git diff 检查通过。

## v0.4.0-docker.2 — 2026-09-21

- 与 `v0.4.0-preview.2` 使用同一源码，继续提供 `linux/amd64` 与 `linux/arm64` 多架构 Docker 镜像。
- Docker 镜像由 GitHub Actions 构建并发布到 GitHub Container Registry；本机未安装 Docker CLI，未在本地重复构建。

## v0.4.0-docker.1 — 2026-09-21

- 增加面向公开 GitHub 仓库的 Docker 看板部署文件、运行状态卷和 Docker 构建校验 workflow。
- 通过 `.env.example` 配置容器代理；报告、决策记录、影子样本和运行缓存继续保持本机私有。
- 发布 `linux/amd64` 与 `linux/arm64` 多架构镜像到 GitHub Container Registry。

## v0.4.0-preview.1 — 2026-09-10

当前开发预览版。

### 主要变化

- 网络路径改为代码内实测择优：并发测试直连、本机候选代理端口、环境代理和 macOS 系统代理，按真实可用延迟选择路径；候选端口统一由 `daily-stock-analysis/scripts/proxy_ports.json` 配置。
- 修复 GUI、实时看板和 `.command` 启动器中的本机绝对路径，输出目录按项目根目录解析，克隆到其他位置后可运行。
- 将 `筛选结果/`、`决策记录/`、持仓、影子样本、运行缓存、日志和旧备份加入共享 `.gitignore`，避免个人运行数据进入 GitHub。
- 增加项目级 `AGENTS.md`、盘中 Skill 入口、ggp 显示/机器输出契约，以及对应的渲染和校验工具。
- 统一规则配置、公告风险状态、观察池状态机和一致性检查，保留“发现工具不负责最终交易裁决”的边界。
- README、CLAUDE 和启动器统一说明代理配置、版本通道及 Windows 尚未适配的限制。

### 验证

- 113 项单元测试通过。
- 一致性检查 26 PASS、0 FAIL、0 WARN。
- Python 语法、Shell 语法和 Git diff 检查通过。

### 已知限制

- 当前仍以 macOS 为主要支持环境，未完成 Windows 适配。
- 盘中网络稳定性和策略效果仍需继续积累样本验证。
- 工具只负责筛选、证据发现和数据查询，不自动下单，也不替代人工交易裁决。

## v0.3.5-preview.1 — 2026-08-30

线上预览基线版本。

- 整理盘中 Skill、筛选引擎、规则配置和一致性检查工具。
- 增加基本面查询、影子验证和报告审计相关能力。
- 记录网络代理依赖、保存路径和 Windows 适配等后续问题，作为 v0.4.0-preview.1 的对照基线。

## v0.3.4 — 2026-08-24

历史版本，保留原有标签；该版本开始明确记录 Windows 尚未适配的限制。
