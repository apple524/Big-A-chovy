# 贡献指南

感谢你对本项目的关注。请先阅读 README 的[使用边界与社区规范](README.md#十二使用边界与社区规范)。

## 项目定位

本项目是本地自用的开源行情研究、数据处理、规则筛选和复盘工具。贡献应服务于代码质量、数据可靠性、可复现性和文档维护，不构成也不提供证券投资咨询或荐股服务。

## 社区讨论范围

Issue、Pull Request 和 Discussion 仅接受以下主题：

- 筛选、解析、数据源适配、缓存、网络路径和看板的技术问题；
- 测试、错误修复、可复现性、性能、可访问性和文档改进；
- 本地部署、Docker、日志和开发工具问题。

## 三条社区底线

1. **不讨论具体买卖**：不讨论具体股票的买入、卖出、持仓、仓位、目标价、止损价、实时交易判断或收益预测。
2. **不接自动交易 PR**：不接受自动交易、券商交易接口、自动生成或下达订单、自动报单/撤单、调仓、代客理财或账户管理相关改动。
3. **不提交敏感数据**：不提交账户、持仓、交易金额、个人决策记录、API 密钥、访问令牌或其他隐私数据；日志和样例必须先脱敏。

以下内容也不在社区范围内：

- “推荐一只股票”“今天能不能买”“明天会不会涨”等个股或市场预测；
- 收费荐股、会员服务、收益分成、共享账户或账户代操作功能。

**本项目不接受自动交易或代客交易相关 PR。**这包括通过券商 API、网页自动化、桌面自动化或其他方式实现的下单、撤单、调仓和账户操作。

## 提交 Issue

请使用对应的 Issue 模板，并提供最小可复现信息。不要粘贴真实持仓、账户截图、交易金额、个人决策记录、API 密钥或其他隐私数据；日志和样例请先脱敏。

如果问题本质上是买卖决策或个股咨询，请不要提交 Issue。本项目不会在 Issue、PR 或 Discussion 中提供此类答复。

## 提交 Pull Request

提交前请确认：

- 改动不包含 `筛选结果/`、`决策记录/`、`holdings.json`、影子样本、账户资料或密钥；
- 改动不新增自动交易、代客理财或账户操作能力；
- 新增行为有相应测试，且没有把筛选候选表述为收益保证或个性化建议；
- 已运行与改动相关的测试，并在 PR 描述中说明结果和已知限制。

## 开发提交流程

1. 从最新的 `main` 创建分支，并在分支中完成单一、可描述的改动。
2. 修改代码、数据处理、测试或文档；不要把本地运行数据、个人决策记录或密钥带入工作区提交。
3. 运行与改动相关的测试，并执行 `git diff --check`；提交前检查 `git diff --cached --name-only`。
4. 提交 Pull Request，说明改动目的、实现范围、测试结果和已知限制；涉及行为变化时同时更新文档和测试。
5. 等待维护者审查。超出社区范围、违反三条底线或无法脱敏的提交可能被关闭或拒绝合并。

维护者可以关闭超出项目范围、包含敏感数据或违反上述边界的 Issue 和 Pull Request。对 fork、二次开发或自行部署版本的运营行为，由相应运营者自行负责。
